export default (state = [], action) => {
    switch (action.type) {
        // case 'GET_POSTS':
        //     state = action.posts;
        //     break
        default:
            return state;
    }
}